name: Deploy Backend to Cloud Run

on:
  push:
    branches: ["main"]
    paths:
      - "client/**"
      - ".github/workflows/deploy-frontend.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1) Auth to GCP using the JSON key secret
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # 2) Install gcloud
      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # 3) Configure Docker for Artifact Registry
      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker europe-central2-docker.pkg.dev --quiet

      # 4) Build frontend image (use client/Dockerfile, repo root as context)
      - name: Build frontend image
        run: |
            IMAGE="europe-central2-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/open-shelf-repo/open-shelf-frontend:${{ github.sha }}"
            echo "IMAGE=$IMAGE" >> $GITHUB_ENV
            docker build -t "$IMAGE" -f client/Dockerfile .

      # 5) Push frontend image
      - name: Push frontend image
        run: |
          docker push "$IMAGE"

      # 6) Deploy to Cloud Run service "open-shelf-frontend"
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy open-shelf-frontend \
            --image "$IMAGE" \
            --region "${{ secrets.GCP_REGION }}" \
            --platform managed \
            --allow-unauthenticated
